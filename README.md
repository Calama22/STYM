# STYM — version mono-fichier de ON AIR TRAIN

> ⚠️ **Repo legacy.** Ce repo contient lapremière version mono-fichier (un seul `index.html` de 213 KB)  de l’app fitness **ON AIR TRAIN**.

## ➜ Pour la version maintenue, voir [train-app](https://github.com/Calama22/train-app)

`train-app` est la version modulaire (HTML / CSS / JS séparés en `core/`, `screens/`, `features/`, `data/`) — toujours déployée sur GitHub Pages.

## Pourquoi garder STYM ?

- Historique : pour retrouver la version "tout-en-un" avant le split en modules
- Référence rapide : ouvrir `index.html` directement dans le navigateur

Pas de PR ni d’issue ici — toute évolution se fait sur train-app.
